# Copy to .env.local for production builds, or set in Render/Railway static site env.
# No trailing slash. Leave empty for local dev (Vite proxies /api to port 5000).
VITE_API_URL=https://your-api.onrender.com
