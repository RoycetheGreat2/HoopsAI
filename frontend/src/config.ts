/** First date included in live tracking and UI (deploy cutoff). */
export const TRACKING_SINCE = '2026-05-22';

/** How far ahead users can browse games (days after today). */
export const SCHEDULE_AHEAD_DAYS = 30;
